#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, L, K;
    cin >> N >> L >> K;
    vector<int> A(N);
    
    for(int i=0; i<N; i++) cin >> A.at(i);

    /* binary search in ans & */
}